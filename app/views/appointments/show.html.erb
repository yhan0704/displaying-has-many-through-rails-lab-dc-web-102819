<!-- shows the date, time, patient, and doctor for that particular appointment. -->
<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br><br>
<%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %><br><br>
<%= link_to @appointment.patient.name, patient_path(@appointment.patient) %>
